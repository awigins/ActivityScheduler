<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.activity.mapper.activity.ActivityMapper">
    <insert id = "insert">
        INSERT INTO activity(
            activity_id, activity_title, `desc`,
            start_time, end_time, creation_time, last_update_time,
            activity_uuid, activity_state_id, activity_admin_user_id
        )
        values (
            #{activityId}, #{activityTitle}, #{description},
            #{startTime}, #{endTime}, #{creationTime}, #{lastUpdateTime},
            #{UUID}, #{activityState.stateId}, #{adminUser.userId}
        );
    </insert>
    <update id = "update">
        UPDATE activity
        <set>
            <if test="activityTitle != null">
                activity_title = #{activityTitle}
            </if>
            <if test="description != null">
                `desc` = #{description}
            </if>
            <if test="startTime != null">
                start_time = #{startTime}
            </if>
            <if test="endTime != null">
                end_time = #{endTime}
            </if>
            last_update_time = #{lastUpdateTime}
        </set>
        <where>
            activity_id = #{activityId}
        </where>
        ;
    </update>
</mapper>